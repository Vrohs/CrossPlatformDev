/**
 * @format
 */

import {AppRegistry} from 'react-native';
import DynamicTheming from './dynamicThemes';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => DynamicTheming);
